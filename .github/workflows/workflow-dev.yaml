on:
  push:
  workflow_dispatch:

jobs:
  check:
    name: githubaction check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Check install 
        run: ls -l . && ./tf.sh install && which bash && env
        shell: bash
